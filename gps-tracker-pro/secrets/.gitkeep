# Optional Docker secrets (alternative to .env.production env vars).
# Copy examples, fill real values, chmod 600, then:
#   docker compose -f docker-compose.production.yml -f docker-compose.secrets.yml --env-file .env.production up -d
#
# Non-secret config (DOMAIN, VITE_*, etc.) still comes from .env.production.
